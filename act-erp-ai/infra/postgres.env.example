# Copy to infra/postgres.env. Use the SAME password in apps/web/.env.prod's
# DATABASE_URL below. Generate: openssl rand -base64 24
POSTGRES_USER=act
POSTGRES_PASSWORD=replace-with-a-strong-random-password
POSTGRES_DB=act
