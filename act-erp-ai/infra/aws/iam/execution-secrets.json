{
  "Version": "2012-10-17",
  "Comment": "Attach to the ECS execution role so tasks can read app secrets at start. Replace <REGION>/<ACCOUNT_ID>.",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["secretsmanager:GetSecretValue"],
      "Resource": ["arn:aws:secretsmanager:<REGION>:<ACCOUNT_ID>:secret:act-erp-ai/*"]
    }
  ]
}
